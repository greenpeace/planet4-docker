dbUsername: '${WP_DB_USERNAME}'
dbPassword: '${WP_DB_PASSWORD}'
dbDatabase: '${WP_DB_DATABASE}'
newrelic:
  license: '${NEWRELIC_LICENSE}'
sqlproxy:
  serviceAccountKey: '${SQLPROXY_KEY}'
wp:
  authKey: '${WP_AUTH_KEY}'
  authSalt: '${WP_AUTH_SALT}'
  loggedInKey: '${WP_LOGGED_IN_KEY}'
  loggedInSalt: '${WP_LOGGED_IN_SALT}'
  nonceKey: '${WP_NONCE_KEY}'
  nonceSalt: '${WP_NONCE_SALT}'
  secureAuthKey: '${WP_SECURE_AUTH_KEY}'
  secureAuthSalt: '${WP_SECURE_AUTH_SALT}'
  stateless:
    serviceAccountKey: '${WP_STATELESS_KEY}'
