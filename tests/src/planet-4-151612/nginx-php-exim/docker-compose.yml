version: '3'
services:
  app:
    image: ${IMAGE_NAMESPACE}/${BATS_PROJECT_ID}/nginx-php-exim:${IMAGE_TAG:-develop}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./persistence/ssl:/etc/nginx/ssl:cached
    environment:
      - APP_HOSTNAME=${APP_HOSTNAME}
      - PHP_MEMORY_LIMIT=${PHP_MEMORY_LIMIT}
      - UPLOAD_MAX_SIZE=${UPLOAD_MAX_SIZE}
