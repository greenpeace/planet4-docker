---
version: '3'
services:
  app:
    build:
      context: geoip
    network_mode: "${NETWORK_MODE}"
    environment:
      - GEOIP2_ENABLED=true
      - GEOIP_LICENSE=tsDyjvwURivDEK4F
      - GEOIP_ACCOUNTID=146520
