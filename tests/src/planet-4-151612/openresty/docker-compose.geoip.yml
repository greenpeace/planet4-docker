---
version: '3'
services:
  app:
    build:
      context: geoip
    network_mode: "${NETWORK_MODE}"
    environment:
      - GEOIP2_ENABLED=true
      - MAXMIND_LICENSE=tsDyjvwURivDEK4F
      - MAXMIND_ACCOUNTID=146520
